<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Музей Будущего</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            width: 100%;
            overflow-x: hidden;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: url('https://github.com/dragoflu/-/blob/main/2026-01-15%2019.21.54.jpg?raw=true') no-repeat center center;
            background-size: cover;
            min-height: 100vh;
            min-height: 100dvh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 20px 16px 30px;
        }

        .container {
            width: 100%;
            max-width: 400px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        /* Corgi Medal Section */
        .corgi-section {
            position: relative;
            width: 280px;
            height: 280px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .medal-background {
            position: absolute;
            width: 100%;
            height: 100%;
            object-fit: contain;
            z-index: 1;
        }

        .corgi-image {
            position: absolute;
            width: 65%;
            height: 65%;
            object-fit: contain;
            z-index: 2;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        /* Buttons Grid */
        .buttons-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            width: 100%;
            max-width: 320px;
            padding: 0 10px;
        }

        .city-button {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 16px;
            color: #1a1a2e;
            background: linear-gradient(180deg, #FF8C42 0%, #FF6B1A 100%);
            border: none;
            border-radius: 16px;
            padding: 16px 20px;
            cursor: pointer;
            transition: all 0.2s ease;
            box-shadow: 
                0 4px 15px rgba(255, 107, 26, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
            text-align: center;
            min-height: 56px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .city-button:hover {
            transform: translateY(-2px);
            box-shadow: 
                0 6px 20px rgba(255, 107, 26, 0.5),
                inset 0 1px 0 rgba(255, 255, 255, 0.3);
        }

        .city-button:active {
            transform: translateY(0);
            box-shadow: 
                0 2px 10px rgba(255, 107, 26, 0.4),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }

        .city-button.light {
            background: linear-gradient(180deg, #FFB88C 0%, #FFA066 100%);
            color: #1a1a2e;
        }

        /* Bottom Museum Section */
        .museum-section {
            margin-top: auto;
            width: 100%;
            max-width: 320px;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
            padding-top: 30px;
        }

        .museum-title {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 22px;
            color: #ffffff;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
            letter-spacing: 1px;
        }

        .museum-badge {
            width: 100%;
            max-width: 280px;
            height: auto;
        }

        /* Progress bar decoration */
        .progress-decoration {
            width: 100%;
            max-width: 300px;
            height: 4px;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(138, 43, 226, 0.6) 20%, 
                rgba(75, 0, 130, 0.8) 50%, 
                rgba(138, 43, 226, 0.6) 80%, 
                transparent 100%
            );
            border-radius: 2px;
            margin-top: 15px;
            position: relative;
        }

        .progress-decoration::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.3) 50%, 
                transparent 100%
            );
            animation: shimmer 2s infinite;
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        /* Responsive adjustments */
        @media (max-height: 700px) {
            .corgi-section {
                width: 220px;
                height: 220px;
            }
            
            .city-button {
                padding: 12px 16px;
                font-size: 14px;
                min-height: 48px;
            }
            
            .museum-section {
                padding-top: 15px;
            }
        }

        @media (min-height: 800px) {
            .container {
                gap: 30px;
            }
            
            .corgi-section {
                width: 300px;
                height: 300px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Corgi in Medal -->
        <div class="corgi-section">
            <img 
                src="https://github.com/dragoflu/-/blob/main/IMG_1641.PNG?raw=true" 
                alt="Medal background" 
                class="medal-background"
            >
            <img 
                src="https://github.com/dragoflu/-/blob/main/IMG_1638.PNG?raw=true" 
                alt="Corgi mascot" 
                class="corgi-image"
            >
        </div>

        <!-- City Buttons -->
        <div class="buttons-container">
            <button class="city-button" onclick="navigateTo('moscow')">
                Москва
            </button>
            <button class="city-button light" onclick="navigateTo('spb')">
                Санкт-Петербург
            </button>
            <button class="city-button" onclick="navigateTo('kaliningrad')">
                Калининград
            </button>
            <button class="city-button light" onclick="navigateTo('serebrinsk')">
                Серебринск
            </button>
        </div>

        <!-- Progress Decoration -->
        <div class="progress-decoration"></div>

        <!-- Museum Section -->
        <div class="museum-section">
            <h1 class="museum-title">Музей Будущего</h1>
            <img 
                src="https://github.com/dragoflu/-/blob/main/IMG_1640.PNG?raw=true" 
                alt="Museum badge" 
                class="museum-badge"
            >
        </div>
    </div>

    <script>
        // Navigation function for buttons
        function navigateTo(city) {
            // Placeholder for future navigation
            // Replace these URLs with actual page URLs when ready
            const routes = {
                'moscow': '#moscow',
                'spb': '#spb',
                'kaliningrad': '#kaliningrad',
                'serebrinsk': '#serebrinsk'
            };
            
            console.log('Navigating to:', city);
            // Uncomment and modify when pages are ready:
            // window.location.href = routes[city];
            
            // Visual feedback
            event.target.style.transform = 'scale(0.95)';
            setTimeout(() => {
                event.target.style.transform = '';
            }, 150);
        }
    </script>
</body>
</html>
